#include <iostream>
#include <vector>
#include <unordered_set>

using namespace std;

vector<vector<int>> solve(int n, int k, vector<int> &a) {


	return {l, r};
}

int main() {
	int t;
	cin >> t;

	while (t--) {
		int n, k;
		cin >> n >> k;
		vector<int> a(2 * n);

		for (int i = 0; i < 2 * n; ++i) {
			cin >> a[i];
		}

		auto result = solve(n, k, a);

	}

	return 0;
}

