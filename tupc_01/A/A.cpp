#include <bits/stdc++.h>

using namespace std;

int main(){
	ios_base::sync_with_stdio(false), cin.tie(nullptr);

    int t;
    cin >> t;
    while(t--){
      int n;
      cin >> n;
      vector<int> seq;
      for(int i=0;i<n;++i){
        int num;
        cin >> num;
        seq.push_back(num);
      }
  
      
    }

	return 0;
}
